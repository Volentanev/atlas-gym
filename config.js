/* Optional: bake your Supabase project in here so every device is auto-configured.
   Leave blank to instead enter them in-app (Settings → Cloud Sync). The anon key is
   safe to expose publicly — row-level security is what protects your data. */
window.ATLAS_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: ""
};
